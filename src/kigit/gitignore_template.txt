# KiCad temporary and backup files
*.000
*.bak
*.bck
*.kicad_pcb-bak
*.kicad_sch-bak
*-backups
*-cache*
*-bak
*-bak*
*~
~*
_autosave-*
#auto_saved_files#
fp-info-cache
~*.lck

# KiCad local history and local settings
.history
*.kicad_prl

# KiGit local backups (created during safe pull/merge)
.kigit-backups/

# Netlist and autorouter exports
*.net
*.dsn
*.ses

# NOTE:
# KiGit can auto-export artifacts into `git-exports/` and commit them.
# If you prefer NOT to version exports, add rules like:
#   git-exports/

# Local-only KiGit settings (do not commit)
.kigit/

# KiGit local conflict backups (do not commit)
# .kigit-backups/
#   *.csv
#   *.pdf
